export const metadata = {
  title: "Configuration GPT Léa — MyBotIA",
  description: "Page d'installation et de configuration du GPT « Léa — MyBotIA » dans GPT Builder.",
};

export default function GptLeaSetupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
